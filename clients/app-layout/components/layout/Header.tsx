const Header = () => {

    return (

        <header
            className="w-full grid grid-cols-[auto_1fr_auto] py-3 px-5 gap-3"
        >
            {/* Logo section */}
            <nav>Logo</nav>

            {/* Main menu section */}
            <nav>Main Menu</nav>

            {/* Other menu section */}
            <nav>Other Menu</nav>
        </header>

    )
}

export default Header