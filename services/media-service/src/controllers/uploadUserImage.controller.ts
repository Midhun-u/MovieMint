import type { Request , Response} from "express";

// Controller for uploading user images
export const uploadUserImageController = async (request: Request, response: Response): Promise<any> => {

    

}