import { Context } from "hono";
import { sendErrorResponse } from "../../utils/sendErrorResponse";

// Controller for getting theater logs
export const getTheaterLogsController = sendErrorResponse(async (context: Context) => {

    

}, "getTheaterLogsController error")